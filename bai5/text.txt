1. LIST
    - list-style-type: chỉ định kiểu đánh dấu mục danh sách
        + list-style-type: disc; Hình tròn màu đen (mặc định)
        + list-style-type: circle; Hình tròn viền đen, trong trắng
        + list-style-type: square; Hình vuông màu đen
        + list-style-type: none; Bị ẩn
        + list-style-type: disclosure-closed;
        + list-style-type: upper-roman; // Chữ la mã (có thể chữ la-tinh)
        + list-style-type: lower-alpha; // Chữ alpha a, b, c
    🔗 https://developer.mozilla.org/en-US/docs/Web/CSS/list-style-type (tham khảo thêm)
    - list-style-position: chỉ định vị trí của các điểm đánh dấu mục danh sách 
        + list-style-position: outside // Các điểm đánh dấu nằm bên ngoài
        + list-style-position: inside // Các điểm đánh dấu nằm bên trong
2. TABLE
3. DISPLAY
    - display: inline; không đặt được chiều cao và chiều rộng, không thêm được margin và padding cho bên trên và bên dưới.
    - display: block; chiều rộng kéo dài từ trái sang phải, luôn bắt đầu ở một hàng mới. Cho phép đặt CD và CR, đặt margin, padding bên trên và bên dưới.
    - display: inline-block; chiều rộng mặc định với chiều rộng element, không bắt đầu ở một hàng mới. Cho phép đặt CD và CR, đặt margin, padding bên trên và bên dưới.
    - display: none; ẩn đi hoàn toàn
    - visibility: hidden; ẩn nhưng vẫn chiếm một khoảng trống
    - visibility: visible; hiển thị - mặc định
4. POSITION
    Thuộc tính position để xác định vị trí element hiển thị.
        + position: static; Vị trí ở trạng thái mặc định của phần tử (tức là các thuộc tính top, bottom, left, right không có hiệu lực).
        + position: relative; Vị trí tương đối so với vị trí mặc định và áp dụng được các thuộc tính top, bottom, right, left.
        + position: absolute; Vị trí tuyệt đối cho các thành phần theo thành phần bao ngoài hoặc cửa sổ trình duyệt.
        + position: fixed; Được định vị so với khung nhìn (viewport), có nghĩa là nó luôn ở một vị trí cố định ngay cả khi trang được cuốn. Các thuộc tính top, bottom, right, left được sử dụng để định vị element.
        + position: sticky; Có thể hiểu đơn giản hơn thì đây là sự kết hợp relative, absolute và fixed. Nó cũng na ná relative nhưng khi scroll đến vị trí của nó sẽ giống hệt như fixed và khi scroll lên khỏi nó thì nó sẽ quay lại vị trí ban đầu dưới dạng relative.
5. Z-INDEX
    - Thiết lập thứ tự xếp chồng nhau của một thành phần vị trí.
    - Giá trị mặc định là 0.
    - Z-Index càng cao thì element đó càng nằm trên.
    - Chú ý: Z-Index chỉ làm việc với các thuộc tính position sau: relative, absolute, fixed, sticky. Đối với static, z-index không thực hiện được.
6. OVERFLOW
    - Xác định điều gì sẽ xảy ra nếu một thành phần bị tràn ra.
    - Có các giá trị sau:
        + overflow: visible; Khi chiều cao của box không đủ chứa text, thì text vẫn hiện tràn qua box, đây là mặc định.
        + overflow: hidden;  Khi chiều cao của box không đủ chứa text, thì text tràn ra sẽ bị ẩn đi.
        Ngoài ra còn có scroll, auto.... (đa số là dùng auto, hidden).
7. Combinator Selectors
    - Adjacent sibling selectors (Bộ chọn anh chị em liền kề)
        + Chọn phần tử tag02 được đặt cùng cấp và phải kề ngay sau phần tử tag01 (mỗi tag01 chỉ có một tag02 ngay sau liền kề).
        + VÍ DỤ: tag01 + tag02 {Viết css} -> tag02 thay đổi
    - General sibling selectors (Bộ anh chị em chung)
        + Chọn tất cả phần tử tag02 cùng cấp với phần tử tag01, những phần tử tag02 phải nằm sau phần tử tag01.
        + VÍ DỤ: tag01 ~ tag02 {Viết css} -> các phần tử tag02 sẽ thay đổi
    - Child selectors (Bộ chọn con)
        + Chọn tất cả phần tử tag02 là cấp con đầu tiên (trực tiếp) của phần tử cha tag01.
        + VÍ DỤ: tag01 > tag02 {}
    - Descendant selectors (Bộ chọn hậu duệ)
        + Chọn tất cả phần tử tag02 các cấp có trong phần tử tag01.
        + VÍ DỤ: tag01 tag02 {}
8. Pseudo-class Selectors (Bộ chọn lớp giả)
    - Dùng để xác định trạng thái đặc biệt của một element.
    - CÚ PHÁP: selector:Pseudo-class {
                    property: value;
                }
    - Một số pseudo phổ biến:
        + :link: khi liên kết chưa truy cập lần nào. (Chỉ dùng cho thẻ a)
        + :visited: khi liên kế đã được truy cập sau lần đầu tiên. (Chỉ dùng cho thẻ a)
        + :hover: khi di chuyển chuột lên element.
        + :active: khi phần tử được click vào, áp dụng cho tất cả loại thẻ.
        + :first-child: thiết lập thuộc tính cho element đầu tiên.
        + :last-child: thiết lập thuộc tính cho element cuối cùng.
        + :nth-child(n): n có thể là các giá trị even (chẵn), odd (lẻ), một số, một biểu thức (an + b). 
9. Pseudo-element Selectors (Bộ chọn phần tử giả)
    - Được sử dụng để tạo ra một phần tử giả và định kiểu (style) cho phần tử giả đó mà không cần tạo phần tử thật.
    - CÚ PHÁP: selector::pseudo-element {
                    property: value;
                }
    - Danh sách Pseudo-element:
        + ::before: Chèn nội dung nào đó trước nội dung của mỗi element. (Phải thêm content:"");
        + ::after: Chèn nội dung nào đó sau nội dung của mỗi element. (Phải thêm content:"");
        + ::first-letter: Chọn chữ cái đầu tiên của mỗi element.
        + ::first-line: Chọn dòng đầu tiên của mỗi element.
        + ::marker: Chọn các điểm đánh dấu của các mục danh sách thẻ (thẻ <ul> và <ol>).
        + ::selection: Được dùng để style cho một vùng văn bản được người dùng chọn (hay còn gọi là "bôi đen").
        Chỉ có một số thuộc tính css khả dụng với ::selection là color, background, cursor và outline.
10. Opacity (Độ mờ)
    - Thuộc tính Opacity chỉ định độ mờ/ độ trong suốt của một element.
    - Thuộc tính Opacity chỉ nhận giá trị từ 0 đến 1. Giá trị càng thấp thì element càng mờ.
    - CÚ PHÁP: opacity: 0.6; Element sẽ mời đi 40%.